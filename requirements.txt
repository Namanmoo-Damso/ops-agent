# LiveKit AI Agents framework
livekit-agents[mcp]==1.3.11
livekit-plugins-aws==1.3.11
livekit-plugins-openai==1.3.11
livekit-plugins-noise-cancellation==0.2.5

# Local VAD (CPU) - lightweight, stable, well-tested with AgentSession
# VAD runs on CPU (fast enough), STT/TTS offloaded to GPU AI Server
livekit-plugins-silero==1.3.11
livekit-plugins-turn-detector==1.3.11

# HTTP client for API calls
httpx==0.28.1

# Redis client for caching
redis==5.3.1

# WebSocket client for External VAD streaming
websockets>=12.0